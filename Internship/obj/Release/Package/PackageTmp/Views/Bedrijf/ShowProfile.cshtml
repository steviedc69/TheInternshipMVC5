@model Internship.Models.Domain.Bedrijf

@{
    ViewBag.Title = "ShowProfile";
    Layout = "~/Views/Bedrijf/_BedrijfLayout.cshtml";
}


    <div class="panel panel-default col-lg-5">
<div class="panel-heading row"><div class="col-xs-12 col-md-8">
    <h2>@Model.Bedrijfsnaam</h2></div>
    <div class="col-xs-6 col-md-4">
        <a href="#" class="thumbnail">
            @if (@Model.ImageUrl != null)
            {
                <img data-src="holder.js/100%x180" alt="logo" src="@Model.ImageUrl">
            }
            else
            {
                <img data-src="holder.js/100%x180" alt="logo" src="/Images/HG.gif">
            }

        </a>
    </div>
</div>
        <div class="panel-body">
            <dl class="dl-horizontal">
                <dt>Activiteit : </dt><dd>@Model.Activiteit</dd>
                
                <dt>Bereikbaarheid : </dt>
                <dd><ul>
                    @if(@Model.PerAuto)
                    {
                        <li>Bereikbaar per auto</li>
                    }
                    @if(@Model.Openbaarvervoer)
                    {
                        <li>Bereikbaar met het openbaar vervoer</li>
                    }
                    </ul></dd>
                <dt>Website : </dt><dd>@Model.Url </dd>

               
                    <dt>Adres : </dt><dd><br />
                    @Model.Adres.StraatNaam  @Model.Adres.Nummer<br />
                    @Model.Adres.Gemeente.Structuur<br/>
                                         <abbr title="Phone">Tel :</abbr> @Model.Telefoon <br />
                </dd>
                </p>
            </dl>
        </div>
        <div class="panel-footer">
            <a href="@Url.Action("EditProfile","Bedrijf",new{id=Model.Id})">Wijzig deze gegevens</a>
        </div>
    </div>


